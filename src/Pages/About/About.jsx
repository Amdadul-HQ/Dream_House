import { Helmet } from "react-helmet-async";

const About = () => {
    return (
        <section>
            <Helmet>
                <title>About Us</title>
            </Helmet>
        </section>
    );
};

export default About;